class SettingsReader {
    public Settings read() {
        Settings settings = new Settings();
        settings.setA(1);
        settings.setB(2);
        settings.setC(4);
        return settings;
    }
}